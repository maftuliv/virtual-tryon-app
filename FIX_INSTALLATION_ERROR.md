# ИСПРАВЛЕНИЕ ОШИБКИ УСТАНОВКИ

## Проблема
При установке зависимостей возникла ошибка с несовместимостью версий пакетов.

## ✅ РЕШЕНИЕ (Автоматическое)

Файлы уже исправлены! Просто выполните:

### Вариант 1: Перезапустите bat-файл
```
start_server.bat
```
Теперь он автоматически обновит pip и установит зависимости.

### Вариант 2: Ручная установка

1. Откройте командную строку
2. Перейдите в папку проекта:
```bash
cd C:\Users\ivmaf\virtual-tryon-app
```

3. Создайте виртуальное окружение (если еще не создано):
```bash
python -m venv venv
```

4. Активируйте его:
```bash
venv\Scripts\activate
```

5. Обновите pip:
```bash
python -m pip install --upgrade pip
```

6. Установите зависимости:
```bash
pip install --no-cache-dir -r requirements.txt
```

### Вариант 3: Минимальная установка

Если Вариант 2 не работает, установите только необходимое:

```bash
pip install Flask Flask-CORS Pillow requests gradio-client
```

## ✅ Проверка установки

После установки проверьте:
```bash
python test_installation.py
```

## 🚀 Запуск сервера

После успешной установки:
```bash
python backend\app.py
```

Или просто запустите:
```
start_server.bat
```

## 📝 Что было исправлено

1. ✅ requirements.txt - изменены версии пакетов на более гибкие
2. ✅ start_server.bat - добавлено обновление pip и альтернативная установка
3. ✅ Добавлен флаг --no-cache-dir для чистой установки

## 💡 Если проблема остается

### Проблема: Ошибка с setuptools

**Решение:**
```bash
pip install --upgrade setuptools wheel
pip install -r requirements.txt
```

### Проблема: Ошибка с конкретным пакетом

**Решение:**
```bash
pip install --upgrade pip setuptools wheel
pip install Flask
pip install Flask-CORS
pip install Pillow
pip install requests
pip install gradio-client
```

### Проблема: Нет интернета или проблемы с PyPI

**Решение:** Скачайте пакеты offline:
```bash
pip download -r requirements.txt -d packages
pip install --no-index --find-links=packages -r requirements.txt
```

## ✅ Готово!

После установки зависимостей:
1. Запустите сервер: `python backend\app.py`
2. Откройте frontend: `frontend\index.html`
3. Используйте приложение!

---

**Дата исправления:** Ноябрь 2024
**Статус:** Исправлено ✅
