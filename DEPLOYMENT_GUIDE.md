# 🚀 ПОЛНОЕ РУКОВОДСТВО ПО РАЗВЕРТЫВАНИЮ НА СЕРВЕРЕ

## Пошаговая инструкция по размещению виртуальной примерочной на сервере с доменом

---

## 📋 СОДЕРЖАНИЕ

1. [Выбор хостинга](#выбор-хостинга)
2. [Регистрация домена](#регистрация-домена)
3. [Подготовка проекта](#подготовка-проекта)
4. [Развертывание на сервере](#развертывание)
5. [Настройка домена](#настройка-домена)
6. [SSL сертификат](#ssl-сертификат)
7. [Запуск в production](#запуск-в-production)

---

## 🌐 ВАРИАНТ 1: HEROKU (БЕСПЛАТНО/ДЕШЕВО) - РЕКОМЕНДУЕТСЯ ДЛЯ НАЧАЛА

### Стоимость:
- **Бесплатно:** 550-1000 часов в месяц (Eco Dyno)
- **Платно:** $5-7/месяц (Basic)
- **Домен:** $10-15/год

### ✅ Преимущества:
- Простое развертывание
- Автоматические обновления
- HTTPS из коробки
- Поддержка Python

---

## 🎯 ПОШАГОВАЯ ИНСТРУКЦИЯ - HEROKU

### Шаг 1: Регистрация на Heroku

1. **Перейдите на:** https://heroku.com
2. **Нажмите:** "Sign Up"
3. **Заполните форму:**
   - Email
   - Пароль
   - Страна
4. **Подтвердите email**
5. **Войдите в аккаунт**

---

### Шаг 2: Установка Heroku CLI

**Для Windows:**

1. **Скачайте:** https://devcenter.heroku.com/articles/heroku-cli
2. **Запустите установщик:** `heroku-x64.exe`
3. **Следуйте инструкциям установщика**

**Проверка установки:**
```bash
heroku --version
```

Должны увидеть: `heroku/8.x.x`

---

### Шаг 3: Подготовка проекта для Heroku

#### 3.1 Откройте командную строку в папке проекта

```bash
cd C:\Users\ivmaf\virtual-tryon-app
```

#### 3.2 Войдите в Heroku

```bash
heroku login
```

Откроется браузер, нажмите "Log In"

#### 3.3 Создайте необходимые файлы

**А) Создайте файл `Procfile` (без расширения!):**

```bash
echo web: gunicorn backend.app:app > Procfile
```

**Или создайте вручную файл `Procfile` с содержимым:**
```
web: gunicorn backend.app:app
```

**Б) Создайте файл `runtime.txt`:**

```bash
echo python-3.11.6 > runtime.txt
```

**В) Обновите `requirements.txt`:**

Добавьте в конец файла:
```
gunicorn==21.2.0
python-dotenv==1.0.0
```

**Полный requirements.txt:**
```
Flask>=2.3.0
Flask-CORS>=4.0.0
Pillow>=10.0.0
requests>=2.31.0
Werkzeug>=2.3.0
gradio-client>=0.8.0
numpy>=1.24.0
gunicorn==21.2.0
python-dotenv==1.0.0
```

---

### Шаг 4: Адаптация кода для production

#### 4.1 Создайте новый файл `backend/wsgi.py`:

```python
from app import app

if __name__ == "__main__":
    app.run()
```

#### 4.2 Обновите `backend/app.py`

Замените последние строки:

```python
if __name__ == '__main__':
    import os
    port = int(os.environ.get('PORT', 5000))

    print("\n" + "=" * 60)
    print("  VIRTUAL TRY-ON SERVER - IDM-VTON")
    print("=" * 60)
    print(f"Upload folder:  {UPLOAD_FOLDER}")
    print(f"Results folder: {RESULTS_FOLDER}")
    print("=" * 60)

    app.run(host='0.0.0.0', port=port, debug=False)
```

---

### Шаг 5: Инициализация Git и развертывание

#### 5.1 Инициализируйте Git репозиторий

```bash
git init
git add .
git commit -m "Initial commit - Virtual Try-On App"
```

#### 5.2 Создайте приложение на Heroku

```bash
heroku create your-tryon-app-name
```

Замените `your-tryon-app-name` на уникальное имя (например: `fashion-tryon-ai`)

Вы получите URL: `https://fashion-tryon-ai.herokuapp.com`

#### 5.3 Настройте переменные окружения

```bash
heroku config:set HF_API_TOKEN=your_huggingface_token
```

(Если у вас есть Hugging Face токен)

#### 5.4 Разверните приложение

```bash
git push heroku main
```

Или если ваша ветка называется master:
```bash
git push heroku master
```

Дождитесь завершения (3-5 минут)

#### 5.5 Откройте приложение

```bash
heroku open
```

Или перейдите в браузере:
```
https://your-tryon-app-name.herokuapp.com
```

---

### Шаг 6: Настройка frontend для работы с Heroku

#### 6.1 Обновите `frontend/app.js`

Замените строку:
```javascript
const API_URL = 'http://localhost:5000';
```

На:
```javascript
const API_URL = window.location.hostname === 'localhost'
    ? 'http://localhost:5000'
    : 'https://your-tryon-app-name.herokuapp.com';
```

#### 6.2 Загрузите frontend на Heroku

Heroku автоматически будет отдавать статические файлы.

---

## 🌐 ВАРИАНТ 2: RAILWAY (ПРОСТОЙ И ДЕШЕВЫЙ)

### Стоимость:
- **Бесплатно:** $5 кредитов в месяц
- **Платно:** от $5/месяц (pay-as-you-go)
- **Домен:** Бесплатный поддомен или свой ($10-15/год)

### Преимущества:
- Еще проще чем Heroku
- Современный интерфейс
- Автоматический CI/CD
- Бесплатный SSL

---

## 🚂 ПОШАГОВАЯ ИНСТРУКЦИЯ - RAILWAY

### Шаг 1: Регистрация

1. **Перейдите:** https://railway.app
2. **Нажмите:** "Start a New Project"
3. **Войдите через GitHub**
4. **Подтвердите аккаунт**

### Шаг 2: Подготовка проекта

#### 2.1 Загрузите проект на GitHub

1. **Создайте репозиторий на GitHub:**
   - Перейдите: https://github.com/new
   - Название: `virtual-tryon-app`
   - Public или Private
   - Нажмите "Create repository"

2. **Загрузите код:**

```bash
cd C:\Users\ivmaf\virtual-tryon-app

git init
git add .
git commit -m "Virtual Try-On App"
git branch -M main
git remote add origin https://github.com/ваш-username/virtual-tryon-app.git
git push -u origin main
```

### Шаг 3: Развертывание на Railway

1. **Вернитесь на Railway.app**
2. **Нажмите:** "New Project"
3. **Выберите:** "Deploy from GitHub repo"
4. **Выберите репозиторий:** `virtual-tryon-app`
5. **Подтвердите**

Railway автоматически:
- Обнаружит Python
- Установит зависимости
- Запустит приложение

### Шаг 4: Настройка

1. **Перейдите в:** Settings → Environment Variables
2. **Добавьте:**
   - `PORT`: 8080
   - `HF_API_TOKEN`: ваш_токен (если есть)

3. **В разделе Settings → Domains:**
   - Нажмите "Generate Domain"
   - Получите URL: `your-app.up.railway.app`

### Шаг 5: Готово!

Приложение доступно по адресу:
```
https://your-app.up.railway.app
```

---

## 💻 ВАРИАНТ 3: VPS (ПОЛНЫЙ КОНТРОЛЬ, ДЕШЕВО)

### Рекомендуемые провайдеры:

| Провайдер | Цена/месяц | RAM | CPU | Диск |
|-----------|------------|-----|-----|------|
| **DigitalOcean** | $4-6 | 1GB | 1 | 25GB |
| **Vultr** | $2.50-6 | 512MB-1GB | 1 | 10-25GB |
| **Hetzner** | €4.5 (~$5) | 2GB | 1 | 20GB |
| **Contabo** | €5 (~$5.5) | 4GB | 2 | 50GB |

---

## 🖥️ ПОШАГОВАЯ ИНСТРУКЦИЯ - VPS (DigitalOcean)

### Шаг 1: Создание сервера (Droplet)

1. **Зарегистрируйтесь:** https://digitalocean.com
2. **Получите $200 кредитов** (для новых пользователей)
3. **Нажмите:** "Create" → "Droplets"
4. **Выберите:**
   - **OS:** Ubuntu 22.04 LTS
   - **Plan:** Basic ($6/месяц, 1GB RAM)
   - **Datacenter:** ближайший к вам
   - **Authentication:** SSH keys (или Password)
5. **Нажмите:** "Create Droplet"

### Шаг 2: Подключение к серверу

**Получите IP адрес** вашего droplet (например: 157.230.45.123)

**Подключитесь через SSH:**

**Windows (PowerShell):**
```powershell
ssh root@157.230.45.123
```

Введите пароль (был отправлен на email)

### Шаг 3: Установка зависимостей на сервере

```bash
# Обновление системы
apt update && apt upgrade -y

# Установка Python и pip
apt install python3 python3-pip python3-venv nginx -y

# Установка Git
apt install git -y
```

### Шаг 4: Загрузка проекта

**Вариант А: Через Git (рекомендуется)**

```bash
cd /var/www
git clone https://github.com/ваш-username/virtual-tryon-app.git
cd virtual-tryon-app
```

**Вариант Б: Через SCP (копирование файлов)**

На вашем компьютере:
```bash
scp -r C:\Users\ivmaf\virtual-tryon-app root@157.230.45.123:/var/www/
```

### Шаг 5: Установка зависимостей проекта

```bash
cd /var/www/virtual-tryon-app

# Создать виртуальное окружение
python3 -m venv venv

# Активировать
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
pip install gunicorn
```

### Шаг 6: Настройка Gunicorn (WSGI сервер)

**Создайте файл `gunicorn_config.py`:**

```python
bind = "0.0.0.0:5000"
workers = 2
timeout = 120
accesslog = "/var/log/gunicorn/access.log"
errorlog = "/var/log/gunicorn/error.log"
```

**Создайте папку для логов:**
```bash
mkdir -p /var/log/gunicorn
```

**Тестовый запуск:**
```bash
gunicorn backend.app:app -c gunicorn_config.py
```

Нажмите Ctrl+C для остановки

### Шаг 7: Создание systemd сервиса (автозапуск)

**Создайте файл `/etc/systemd/system/tryon.service`:**

```bash
nano /etc/systemd/system/tryon.service
```

**Вставьте:**

```ini
[Unit]
Description=Virtual Try-On Application
After=network.target

[Service]
User=root
WorkingDirectory=/var/www/virtual-tryon-app
Environment="PATH=/var/www/virtual-tryon-app/venv/bin"
ExecStart=/var/www/virtual-tryon-app/venv/bin/gunicorn backend.app:app -c gunicorn_config.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Сохраните: `Ctrl+X`, затем `Y`, затем `Enter`

**Запустите сервис:**

```bash
systemctl daemon-reload
systemctl start tryon
systemctl enable tryon
systemctl status tryon
```

Должны увидеть: `Active: active (running)`

### Шаг 8: Настройка Nginx (веб-сервер)

**Создайте конфиг `/etc/nginx/sites-available/tryon`:**

```bash
nano /etc/nginx/sites-available/tryon
```

**Вставьте:**

```nginx
server {
    listen 80;
    server_name ваш-домен.com www.ваш-домен.com;

    # Frontend
    location / {
        root /var/www/virtual-tryon-app/frontend;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # Backend API
    location /api/ {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Увеличенные таймауты для обработки изображений
        proxy_connect_timeout 120s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;
    }

    # Статические файлы
    location /static/ {
        alias /var/www/virtual-tryon-app/static/;
    }

    # Увеличить лимит размера файла
    client_max_body_size 20M;
}
```

**Активируйте конфиг:**

```bash
ln -s /etc/nginx/sites-available/tryon /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
```

### Шаг 9: Обновите frontend для работы с API

**Отредактируйте `frontend/app.js`:**

```javascript
const API_URL = window.location.hostname === 'localhost'
    ? 'http://localhost:5000'
    : '';  // Пустая строка = тот же домен
```

---

## 🌐 РЕГИСТРАЦИЯ И НАСТРОЙКА ДОМЕНА

### Шаг 1: Регистрация домена

**Рекомендуемые регистраторы:**

| Регистратор | Цена .com | Цена .ru | Особенности |
|-------------|-----------|----------|-------------|
| **Namecheap** | $8.88/год | - | Дешево, просто |
| **Reg.ru** | 799₽/год | 299₽/год | Русский интерфейс |
| **Cloudflare** | $9.15/год | - | + бесплатный CDN |
| **GoDaddy** | $11.99/год | - | Популярный |

### Шаг 2: Покупка домена (Namecheap)

1. **Перейдите:** https://namecheap.com
2. **Введите желаемое имя:** например `fashion-tryon.com`
3. **Проверьте доступность**
4. **Добавьте в корзину**
5. **Оформите заказ** (~$9-12/год)

### Шаг 3: Настройка DNS

#### Для Heroku:

1. **В Namecheap:**
   - Domain List → Manage → Advanced DNS
   - Добавьте записи:

```
Type    Host    Value                           TTL
CNAME   www     your-app.herokuapp.com          Automatic
CNAME   @       your-app.herokuapp.com          Automatic
```

2. **В Heroku:**
```bash
heroku domains:add fashion-tryon.com
heroku domains:add www.fashion-tryon.com
```

#### Для Railway:

1. **В Railway:**
   - Settings → Domains → Custom Domain
   - Введите: `fashion-tryon.com`
   - Скопируйте CNAME значение

2. **В Namecheap:**
   - Добавьте CNAME запись с полученным значением

#### Для VPS:

1. **В Namecheap:**
   - Advanced DNS → Add New Record

```
Type    Host    Value                   TTL
A       @       157.230.45.123         Automatic
A       www     157.230.45.123         Automatic
```

Замените `157.230.45.123` на IP вашего сервера

2. **Подождите 5-30 минут** для распространения DNS

---

## 🔒 УСТАНОВКА SSL СЕРТИФИКАТА (HTTPS)

### Для Heroku/Railway:
**Автоматически включено! ✅**

### Для VPS (Let's Encrypt - БЕСПЛАТНО):

```bash
# Установка Certbot
apt install certbot python3-certbot-nginx -y

# Получение сертификата
certbot --nginx -d fashion-tryon.com -d www.fashion-tryon.com

# Введите email
# Согласитесь с условиями
# Выберите: Redirect (автоматический редирект на HTTPS)
```

**Автообновление:**
```bash
certbot renew --dry-run
```

Certbot автоматически обновит сертификат каждые 60 дней.

**Готово!** Ваш сайт доступен по HTTPS:
```
https://fashion-tryon.com
```

---

## 📊 СРАВНЕНИЕ ВАРИАНТОВ

| Критерий | Heroku | Railway | VPS |
|----------|--------|---------|-----|
| **Цена** | $5-7/мес | $5/мес | $4-6/мес |
| **Сложность** | ⭐⭐ | ⭐ | ⭐⭐⭐⭐ |
| **Контроль** | Средний | Средний | Полный |
| **SSL** | Авто ✅ | Авто ✅ | Ручная настройка |
| **Масштабируемость** | Легко | Легко | Сложно |
| **Время развертывания** | 10 мин | 5 мин | 30-60 мин |

---

## 💰 ИТОГОВАЯ СТОИМОСТЬ

### Минимальный бюджет:

**Вариант 1: Railway + Reg.ru домен**
- Railway: $5/месяц (или бесплатно первый месяц)
- Домен .ru: 299₽/год (~$3)
- **ИТОГО: ~$5-8/месяц**

**Вариант 2: VPS + Cloudflare**
- VPS Vultr: $3.50/месяц
- Домен: $9/год (~$0.75/месяц)
- **ИТОГО: ~$4-5/месяц**

**Вариант 3: Heroku Eco**
- Heroku Eco Dyno: $5/месяц
- Домен: $9/год (~$0.75/месяц)
- **ИТОГО: ~$6/месяц**

---

## 🎯 РЕКОМЕНДАЦИЯ ДЛЯ НАЧИНАЮЩИХ

### Начните с Railway:

✅ **Почему:**
1. Проще всего настроить (5-10 минут)
2. Бесплатный пробный период
3. Автоматический SSL
4. Современный интерфейс
5. GitHub интеграция

✅ **Как начать:**
1. Загрузите проект на GitHub (5 мин)
2. Подключите к Railway (2 мин)
3. Получите бесплатный поддомен (1 мин)
4. Купите свой домен потом (опционально)

**Общее время: 10-15 минут**

---

## 📝 ФИНАЛЬНЫЙ ЧЕКЛИСТ

### Перед развертыванием:

- [ ] Создан аккаунт на хостинге
- [ ] Установлены необходимые инструменты (Git, Heroku CLI)
- [ ] Созданы файлы: Procfile, runtime.txt
- [ ] Обновлен requirements.txt
- [ ] Код адаптирован для production
- [ ] Проект загружен на GitHub (для Railway)

### Настройка домена:

- [ ] Домен зарегистрирован
- [ ] DNS записи настроены
- [ ] SSL сертификат установлен
- [ ] Домен работает (проверка в браузере)

### Тестирование:

- [ ] Главная страница открывается
- [ ] API отвечает (проверка /api/health)
- [ ] Загрузка файлов работает
- [ ] Обработка изображений работает
- [ ] Скачивание результатов работает

---

## 🆘 РЕШЕНИЕ ПРОБЛЕМ

### Heroku: "Application error"

```bash
heroku logs --tail
```

Смотрите логи и исправляйте ошибки.

### Railway: Build failed

Проверьте:
- requirements.txt правильный
- Python версия поддерживается
- Все файлы загружены на GitHub

### VPS: Nginx 502 Bad Gateway

```bash
systemctl status tryon
journalctl -u tryon -n 50
```

Проверьте что backend запущен.

### Домен не работает

- Подождите 30-60 минут для DNS
- Проверьте DNS: https://dnschecker.org
- Проверьте A/CNAME записи

---

## 📞 ПОЛЕЗНЫЕ ССЫЛКИ

- **Heroku Docs:** https://devcenter.heroku.com/
- **Railway Docs:** https://docs.railway.app/
- **DigitalOcean Tutorials:** https://www.digitalocean.com/community/tutorials
- **Nginx Configuration:** https://nginx.org/en/docs/
- **Let's Encrypt:** https://letsencrypt.org/

---

## 🎉 ПОЗДРАВЛЯЕМ!

После выполнения этих шагов ваша виртуальная примерочная будет:

✅ Доступна в интернете 24/7
✅ Работать по HTTPS
✅ Доступна по вашему домену
✅ Готова к использованию

**Успехов в развертывании! 🚀**

---

*Создано: Ноябрь 2024*
*Версия: 1.0.0*
