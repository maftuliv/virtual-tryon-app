# 📦 Руководство по резервному копированию

## Обзор

В проекте Virtual Try-On реализовано несколько уровней защиты данных:

1. **Git репозиторий** - основной бэкап на GitHub
2. **Локальные бэкапы** - скрипты для создания локальных копий
3. **Railway автоматический деплой** - восстановление из Git

---

## 1️⃣ Основной бэкап: Git + GitHub

### Текущий репозиторий
- **GitHub**: https://github.com/maftuliv/virtual-tryon-app
- Все изменения автоматически сохраняются при `git push`
- История всех коммитов доступна для восстановления

### Восстановление из GitHub

```bash
# Клонировать репозиторий
git clone https://github.com/maftuliv/virtual-tryon-app.git

# Или скачать конкретную версию
git clone --branch main https://github.com/maftuliv/virtual-tryon-app.git
```

### Откат к предыдущей версии

```bash
# Посмотреть историю коммитов
git log --oneline

# Откатиться к конкретному коммиту
git checkout <commit-hash>

# Создать новую ветку из старой версии
git checkout -b backup-branch <commit-hash>
```

---

## 2️⃣ Локальные бэкапы

### Создание бэкапа

Запустите скрипт:
```
backup.bat
```

Что происходит:
- ✅ Создается папка с датой и временем в `../project-backups/`
- ✅ Копируются все файлы проекта (кроме node_modules, venv, uploads)
- ✅ Сохраняется информация о последнем коммите
- ✅ Подсчитывается количество файлов

Пример имени бэкапа:
```
backup_2025-01-13_1430
```

### Восстановление из бэкапа

Запустите скрипт:
```
restore.bat
```

Шаги:
1. Выберите бэкап из списка
2. Подтвердите восстановление (напишите `yes`)
3. Автоматически создастся бэкап текущего состояния перед восстановлением
4. Файлы будут восстановлены

### Расположение бэкапов

```
C:\Users\ivmaf\
├── virtual-tryon-app\          (основной проект)
└── project-backups\            (папка с бэкапами)
    ├── backup_2025-01-13_1430\
    ├── backup_2025-01-13_1500\
    └── backup_2025-01-14_0900\
```

---

## 3️⃣ Бэкап базы данных (если используется PostgreSQL)

### Railway PostgreSQL

Railway автоматически создает бэкапы базы данных. Для ручного экспорта:

```bash
# Установите PostgreSQL клиент
# Затем экспортируйте базу
pg_dump -h <railway-host> -U <username> -d <database> > backup.sql
```

### Восстановление базы данных

```bash
psql -h <railway-host> -U <username> -d <database> < backup.sql
```

---

## 4️⃣ Бэкап важных файлов

### Что нужно обязательно сохранять:

1. **Файлы конфигурации**:
   - `.env` (переменные окружения)
   - Railway переменные окружения
   - Telegram bot token и chat ID

2. **Файлы проекта**:
   - `frontend/` - весь фронтенд
   - `backend/` - весь бэкенд
   - `requirements.txt` - зависимости Python
   - `package.json` - зависимости Node.js (если есть)

3. **Данные пользователей**:
   - `feedback/` - отзывы пользователей (JSON файлы)
   - База данных PostgreSQL

---

## 5️⃣ Автоматический бэкап

У вас есть **ДВА варианта** автоматического бэкапа:

### ☁️ Вариант 1: GitHub Actions (РЕКОМЕНДУЕТСЯ)

**Преимущества:**
- ✅ Работает даже когда компьютер выключен
- ✅ Бэкапы хранятся на серверах GitHub
- ✅ Полностью автоматически
- ✅ Бесплатно для публичных репозиториев
- ✅ Доступ к бэкапам из любого места

**Как настроить:**
```
1. Запустите: setup_github_backup.bat
2. Дождитесь загрузки на GitHub
3. Перейдите на GitHub → вкладка "Actions"
4. Включите GitHub Actions (если попросит)
5. Готово! Бэкап будет каждые 3 дня
```

**Расписание:** Каждые 3 дня в 2:00 AM UTC (5:00 AM по Москве)

**Где хранятся бэкапы:** GitHub в ветках `backup/auto-YYYY-MM-DD`

### 💻 Вариант 2: Локальный бэкап (требует включенного ПК)

Проект включает локальный автоматический бэкап, который:
- ✅ Запускается каждые 3 дня в 2:00 ночи
- ✅ Хранит только последние 3 бэкапа локально
- ✅ Автоматически удаляет старые бэкапы
- ✅ Записывает лог операций
- ⚠️ Требует, чтобы компьютер был включен

### Установка автоматического бэкапа:

1. **Запустите установщик как администратор**:
   - Найдите файл `setup_auto_backup.bat`
   - Правый клик → "Запуск от имени администратора"
   - Подтвердите установку (введите `yes`)

2. **Проверка установки**:
   - Откройте "Планировщик заданий" (Task Scheduler)
   - Найдите задачу "VirtualTryOn_AutoBackup"
   - Убедитесь, что статус "Готово"

3. **Тестирование**:
   ```
   Запустите вручную: auto_backup.bat
   ```

### Удаление автоматического бэкапа:

Если хотите отключить автоматический бэкап:
- Запустите `remove_auto_backup.bat` от имени администратора

### Управление бэкапами:

**Автоматические бэкапы** имеют имена:
```
auto_backup_2025-01-13
auto_backup_2025-01-16
auto_backup_2025-01-19
```

Система хранит только **3 последних** автоматических бэкапа. При создании 4-го бэкапа самый старый удаляется автоматически.

### Лог автоматических бэкапов:

Все операции записываются в файл:
```
../project-backups/backup_log.txt
```

### 🔄 Ручной бэкап на GitHub

Если хотите создать бэкап прямо сейчас и отправить на GitHub:
```
Запустите: backup_to_github.bat
```

Это создаст ветку `backup/local-YYYY-MM-DD` и отправит на GitHub.

### 📊 Просмотр бэкапов на GitHub

**Веб-интерфейс:**
1. Откройте: https://github.com/maftuliv/virtual-tryon-app/branches
2. Найдите ветки с префиксом `backup/`
3. Выберите нужную дату

**Через Git:**
```bash
# Показать все backup ветки
git branch -r | grep backup

# Восстановить конкретный бэкап
git checkout backup/auto-2025-01-13
```

### Расписание бэкапов:

- **GitHub Actions (облако)**: Каждые 3 дня, автоматически
- **Локальный**: Каждые 3 дня (если ПК включен и настроено)
- **Вручную на GitHub**: `backup_to_github.bat` - когда нужно
- **Вручную локально**: `backup.bat` - когда нужно
- **Git push**: После каждого изменения

---

## 6️⃣ Восстановление после сбоя

### Сценарий 1: Сломался код

```bash
# Откатиться к последнему коммиту
git reset --hard HEAD

# Или к конкретному коммиту
git reset --hard <commit-hash>
```

### Сценарий 2: Удалены файлы

```bash
# Восстановить все файлы из Git
git checkout .

# Или запустить restore.bat для восстановления из локального бэкапа
```

### Сценарий 3: Полная переустановка

1. Клонируйте репозиторий с GitHub
2. Установите зависимости:
   ```bash
   # Python
   pip install -r requirements.txt

   # Если есть Node.js зависимости
   npm install
   ```
3. Настройте переменные окружения
4. Восстановите базу данных (если нужно)
5. Задеплойте на Railway

### Сценарий 4: Railway перестал работать

1. Создайте новый проект на Railway
2. Подключите GitHub репозиторий
3. Настройте переменные окружения
4. Railway автоматически задеплоит проект

---

## 7️⃣ Чек-лист перед важными изменениями

- [ ] Создать Git коммит текущего состояния
- [ ] Запустить `backup.bat` для локального бэкапа
- [ ] Проверить, что Railway деплой работает
- [ ] Убедиться, что есть доступ к GitHub
- [ ] Экспортировать базу данных (если используется)

---

## 8️⃣ Контакты и доступы

### Важно сохранить:

- **GitHub**: https://github.com/maftuliv/virtual-tryon-app
- **Railway**: https://railway.app
- **Домен**: taptolook.net
- **Telegram Bot Token**: (сохранен в Railway env)
- **PostgreSQL данные**: (сохранены в Railway)

### Файлы с секретами (хранить отдельно!):

- `.env` файл с токенами
- Railway environment variables
- Пароли от базы данных

---

## 9️⃣ Быстрые команды

```bash
# Создать бэкап
backup.bat

# Восстановить из бэкапа
restore.bat

# Коммит и пуш в Git
git add .
git commit -m "Описание изменений"
git push

# Посмотреть историю
git log --oneline

# Откатиться на один коммит назад
git reset --hard HEAD~1

# Проверить статус
git status
```

---

## 🆘 Экстренное восстановление

Если всё сломалось:

1. **Не паникуйте!** Всё можно восстановить
2. Проверьте GitHub - там полная история проекта
3. Посмотрите локальные бэкапы в `../project-backups/`
4. Railway хранит историю деплоев - можно откатиться
5. В крайнем случае - клонируйте проект заново с GitHub

---

## 📞 Поддержка

Если нужна помощь с восстановлением:
- Проверьте этот файл
- Посмотрите Git log
- Используйте `restore.bat` для восстановления локальных бэкапов
