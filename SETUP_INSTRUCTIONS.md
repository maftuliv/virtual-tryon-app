# Пошаговая инструкция по установке и запуску

## Шаг 1: Проверка системных требований

### 1.1 Проверьте установлен ли Python

Откройте командную строку (cmd) или PowerShell и выполните:

```bash
python --version
```

Должна быть версия Python 3.9 или выше. Если Python не установлен:
- Скачайте с официального сайта: https://www.python.org/downloads/
- При установке обязательно отметьте "Add Python to PATH"

### 1.2 Проверьте установлен ли pip

```bash
pip --version
```

Если pip не установлен, он обычно идет вместе с Python.

## Шаг 2: Установка зависимостей

### 2.1 Откройте командную строку в папке проекта

```bash
cd C:\Users\ivmaf\virtual-tryon-app
```

### 2.2 Создайте виртуальное окружение (рекомендуется)

```bash
python -m venv venv
```

### 2.3 Активируйте виртуальное окружение

**Для Windows (cmd):**
```bash
venv\Scripts\activate
```

**Для Windows (PowerShell):**
```bash
venv\Scripts\Activate.ps1
```

Если возникает ошибка с правами в PowerShell, выполните:
```bash
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 2.4 Установите Python зависимости

```bash
pip install -r requirements.txt
```

Этот процесс может занять несколько минут. Дождитесь завершения установки всех пакетов.

### 2.5 Проверьте установку

```bash
pip list
```

Вы должны увидеть установленные пакеты: Flask, Pillow, requests, gradio-client и др.

## Шаг 3: Подготовка папок

### 3.1 Создайте необходимые папки (если они не созданы)

```bash
mkdir uploads
mkdir results
```

## Шаг 4: Настройка переменных окружения

Перед запуском убедитесь, что критичные переменные окружения заданы. Используйте файл `.env` (копия `.env.example`) или добавьте их в Railway → Variables.

### 4.1 Обязательные переменные

| Переменная | Назначение | Где взять |
|-----------|------------|-----------|
| `DATABASE_URL` | Подключение к PostgreSQL (используется auth и лимиты) | Railway создаёт автоматически или укажите свою БД |
| `JWT_SECRET_KEY` | Подпись JWT-токенов. **Обязательно** задайте уникальное значение. | Сгенерируйте командой `python -c "import secrets; print(secrets.token_urlsafe(64))"` |
| `NANOBANANA_API_KEY` | Основной API-ключ try-on | Кабинет NanoBanana |

### 4.2 Дополнительные переменные

- `FASHN_API_KEY` — альтернативный провайдер генераций.
- `HF_API_TOKEN` — токен HuggingFace (ускоряет валидацию/детекцию).
- `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` — чтобы получать отзывы в Telegram (см. TELEGRAM_SETUP.md).
- `ENABLE_STARTUP_DIAGNOSTICS` — оставьте пустым/`0` на проде. Установите в `1`, только если нужно локально вывести диагностику (ключи будут замаскированы).

После задания переменных **перезапустите backend**. В логах не должно быть предупреждения `[AUTH] ⚠️ JWT_SECRET_KEY not set...`.

## Шаг 5: (Опционально) Настройка Hugging Face API Token

Для улучшенного доступа к модели IDM-VTON:

### 4.1 Получите токен

1. Зарегистрируйтесь на https://huggingface.co/
2. Перейдите в Settings → Access Tokens
3. Создайте новый токен (Read access достаточно)
4. Скопируйте токен

### 4.2 Создайте файл .env

Скопируйте .env.example в .env:
```bash
copy .env.example .env
```

Откройте .env в текстовом редакторе и замените `your_token_here` на ваш токен.

**Примечание:** Этот шаг опциональный. Приложение будет работать и без токена, но может быть медленнее.

## Шаг 6: Запуск Backend сервера

### 5.1 Убедитесь что виртуальное окружение активировано

Вы должны видеть `(venv)` в начале строки командной строки.

### 5.2 Запустите Flask сервер

```bash
python backend\app.py
```

Вы должны увидеть:
```
======================================================
Virtual Try-On Server Starting...
======================================================
Upload folder: C:\Users\ivmaf\virtual-tryon-app\uploads
Results folder: C:\Users\ivmaf\virtual-tryon-app\results
======================================================
* Running on http://0.0.0.0:5000
* Running on http://127.0.0.1:5000
```

### 5.3 Проверьте работу сервера

Откройте браузер и перейдите по адресу: http://localhost:5000

Вы должны увидеть JSON ответ:
```json
{
  "status": "running",
  "message": "Virtual Try-On API Server",
  "version": "1.0.0"
}
```

**НЕ ЗАКРЫВАЙТЕ это окно командной строки!** Сервер должен работать постоянно.

## Шаг 7: Открытие Frontend

### Вариант 1: Открыть HTML файл напрямую

1. Откройте проводник Windows
2. Перейдите в папку `C:\Users\ivmaf\virtual-tryon-app\frontend`
3. Двойным щелчком откройте файл `index.html`
4. Файл откроется в вашем браузере по умолчанию

### Вариант 2: Использовать локальный веб-сервер (рекомендуется)

Откройте **НОВОЕ** окно командной строки (оставив первое с Flask работающим):

```bash
cd C:\Users\ivmaf\virtual-tryon-app\frontend
python -m http.server 8080
```

Затем откройте браузер и перейдите по адресу: http://localhost:8080

## Шаг 8: Использование приложения

### 7.1 Подготовка изображений

Подготовьте:
- **1-4 фото себя:** желательно в полный рост, на светлом фоне, хорошее освещение
- **1 фото одежды:** четкое изображение одежды на прозрачном или белом фоне

### 7.2 Загрузка изображений

1. В разделе "Шаг 1" нажмите на область загрузки или перетащите фото
2. Выберите 1-4 фотографии себя
3. В разделе "Шаг 2" загрузите фото одежды
4. Проверьте превью изображений

### 7.3 Запуск обработки

1. Нажмите кнопку "Примерить одежду"
2. Дождитесь завершения обработки (30-60 секунд)
3. Просмотрите результаты

### 7.4 Сохранение результатов

- Нажмите "Скачать" под каждым результатом для сохранения отдельных фото
- Нажмите "Скачать все результаты" для сохранения всех фото сразу

## Шаг 9: Остановка приложения

## Шаг 10: Проверка лимитов и диагностики

1. **Лимиты без авторизации**
   - В режиме инкогнито сделайте 3 генерации — запросы должны пройти.
   - На 4‑й попытке появится баннер с предложением зарегистрироваться, а кнопка не запустит генерацию.
2. **Сбой проверки лимита**
   - Временно остановите backend или отключите интернет и нажмите «Сделать примерку».
   - Должно появиться сообщение «Не удалось проверить лимит…» под кнопкой, генерация блокируется.
3. **Диагностика окружения**
   - При необходимости установите `ENABLE_STARTUP_DIAGNOSTICS=1` и перезапустите сервер.
   - В логах появится сводка по переменным окружения с маскированными значениями. На проде оставляйте значение пустым.

### 8.1 Остановка Backend сервера

В окне командной строки с Flask сервером нажмите:
```
Ctrl + C
```

### 8.2 Остановка Frontend сервера (если использовали вариант 2)

В окне командной строки с Python HTTP сервером нажмите:
```
Ctrl + C
```

### 8.3 Деактивация виртуального окружения

```bash
deactivate
```

## Решение проблем

### Проблема: "python не распознан как внутренняя или внешняя команда"

**Решение:** Python не добавлен в PATH. Переустановите Python с опцией "Add to PATH".

### Проблема: "No module named 'flask'"

**Решение:** Зависимости не установлены. Выполните:
```bash
pip install -r requirements.txt
```

### Проблема: "Port 5000 already in use"

**Решение:** Порт 5000 уже используется. Измените порт в файле `backend/app.py` (последняя строка):
```python
app.run(host='0.0.0.0', port=5001, debug=True)
```

И в `frontend/app.js` (строка 2):
```javascript
const API_URL = 'http://localhost:5001';
```

### Проблема: "Сервер недоступен" в браузере

**Решение:**
1. Убедитесь что Flask сервер запущен
2. Проверьте что в браузере открыт правильный адрес
3. Проверьте firewall/антивирус

### Проблема: Медленная обработка

**Решение:**
1. Используйте изображения меньшего размера (до 2MB)
2. Получите Hugging Face API токен (Шаг 5)
3. Проверьте скорость интернет-соединения

### Проблема: Ошибка при обработке изображений

**Решение:**
1. Убедитесь что изображения в правильном формате (JPG, PNG)
2. Используйте качественные фото с хорошим освещением
3. Убедитесь что на фото виден человек в полный рост

## Следующие шаги

После успешного запуска вы можете:

1. **Экспериментировать:** Попробуйте разные фото и одежду
2. **Оптимизировать:** Настройте параметры в backend/app.py
3. **Кастомизировать:** Измените дизайн в frontend/style.css
4. **Развернуть:** Разместите на сервере для публичного доступа

## Полезные команды

### Просмотр логов сервера
Все логи отображаются в окне командной строки с запущенным Flask

### Очистка файлов
```bash
# Удалить все загруженные и результаты
del /Q uploads\*
del /Q results\*
```

### Обновление зависимостей
```bash
pip install --upgrade -r requirements.txt
```

## Контакты и поддержка

При возникновении проблем:
1. Проверьте этот файл инструкций
2. Проверьте README.md
3. Проверьте официальную документацию IDM-VTON: https://github.com/yisol/IDM-VTON

Удачи в использовании виртуальной примерочной! 🎉
