# 🔍 Google OAuth Diagnostics Guide

## Проблема: Error 401: invalid_client

Ошибка `401: invalid_client` от Google означает, что Google не может найти OAuth клиент с указанным Client ID.

---

## 🔧 Шаги диагностики

### Шаг 1: Проверка конфигурации через API

Откройте в браузере или выполните curl запрос:

```bash
# Тестовое окружение
curl https://testbackendvirtualtryon-production.up.railway.app/api/auth/google/status
```

**Ожидаемый ответ:**
```json
{
  "enabled": true,
  "configured": true,
  "redirect_uri": "https://testbackendvirtualtryon-production.up.railway.app/api/auth/google/callback",
  "client_id_masked": "1234567890...xyz",
  "client_id_format_valid": true,
  "config_issues": []
}
```

**Если есть проблемы:**
- `config_issues` будет содержать список проблем
- `client_id_format_valid` покажет, правильный ли формат Client ID

### Шаг 2: Проверка переменных окружения в Railway

1. Откройте Railway Dashboard
2. Выберите сервис: `test_backend_virtual_tryon`
3. Перейдите в **Variables** tab
4. Проверьте следующие переменные:

```
GOOGLE_OAUTH_ENABLED=true
GOOGLE_CLIENT_ID=YOUR_CLIENT_ID.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-YOUR_CLIENT_SECRET
GOOGLE_REDIRECT_URI=https://testbackendvirtualtryon-production.up.railway.app/api/auth/google/callback
```

**⚠️ ВАЖНО:**
- Убедитесь, что нет лишних пробелов в начале или конце значений
- Убедитесь, что значения не в кавычках (Railway автоматически обрабатывает строки)
- Проверьте, что Client ID заканчивается на `.apps.googleusercontent.com`

### Шаг 3: Проверка Google Cloud Console

1. Откройте [Google Cloud Console](https://console.cloud.google.com/)
2. Выберите проект: **Tap to look Virtual Try-On**
3. Перейдите: **APIs & Services** → **Credentials**
4. Найдите OAuth 2.0 Client ID (формат: `123456789-abc...xyz.apps.googleusercontent.com`)

**Проверьте:**
- ✅ Client ID существует и активен
- ✅ Client Secret совпадает с тем, что в Railway
- ✅ **Authorized redirect URIs** содержит:
  - `https://testbackendvirtualtryon-production.up.railway.app/api/auth/google/callback`
  - `https://taptolook.net/api/auth/google/callback` (для продакшена)

**⚠️ КРИТИЧНО:** Redirect URI должен совпадать **ТОЧНО**, включая:
- Протокол (https)
- Домен (без www, если не указан)
- Путь (с `/api/auth/google/callback`)
- Без trailing slash

### Шаг 4: Проверка логов Railway

1. В Railway Dashboard откройте **Deployments** → выберите последний deployment
2. Откройте **Logs**
3. Найдите логи с префиксом `[GOOGLE-AUTH]`

**Что искать:**
```
[GOOGLE-AUTH] Google OAuth 2.0 service initialized
[GOOGLE-AUTH] Client ID: 1234567890...xyz
[GOOGLE-AUTH] Redirect URI: https://testbackendvirtualtryon-production.up.railway.app/api/auth/google/callback
```

**Если видите ошибки:**
- `Client ID format may be incorrect` - проверьте формат Client ID
- `GOOGLE_CLIENT_ID is empty` - переменная окружения не установлена
- `Error generating authorization URL` - проблема с конфигурацией

### Шаг 5: Тестирование authorization URL

1. Откройте: `https://testtaptolooknet-production.up.railway.app`
2. Откройте DevTools (F12) → Network tab
3. Нажмите "Войти через Google"
4. Проверьте запрос к `/api/auth/google/login`

**Ожидаемый ответ:**
```json
{
  "success": true,
  "authorization_url": "https://accounts.google.com/o/oauth2/auth?client_id=YOUR_CLIENT_ID.apps.googleusercontent.com&..."
}
```

**Скопируйте `authorization_url` и откройте в браузере:**
- Если видите ошибку `401: invalid_client` - проблема в Client ID
- Если видите consent screen - Client ID правильный, проблема в другом месте

---

## 🐛 Возможные причины и решения

### Причина 1: Client ID не существует в Google Cloud Console

**Симптомы:**
- Ошибка `401: invalid_client` при открытии authorization URL
- Client ID не найден в Google Cloud Console

**Решение:**
1. Проверьте, что вы используете правильный проект Google Cloud
2. Убедитесь, что OAuth Client не был удален
3. Если Client был удален, создайте новый:
   - **APIs & Services** → **Credentials** → **Create Credentials** → **OAuth Client ID**
   - Выберите **Web application**
   - Добавьте Redirect URI
   - Скопируйте новый Client ID и Secret
   - Обновите переменные в Railway

### Причина 2: Client ID принадлежит другому проекту

**Симптомы:**
- Client ID существует, но в другом проекте Google Cloud
- Ошибка `401: invalid_client`

**Решение:**
1. Проверьте, в каком проекте создан Client ID
2. Либо используйте Client ID из правильного проекта
3. Либо создайте новый Client ID в нужном проекте

### Причина 3: Лишние пробелы в переменных окружения

**Симптомы:**
- Client ID выглядит правильным, но Google не принимает
- В логах видно предупреждение о формате

**Решение:**
1. В Railway Variables удалите и заново добавьте переменные
2. Убедитесь, что нет пробелов в начале/конце
3. Перезапустите сервис (Railway сделает это автоматически при изменении переменных)

### Причина 4: Неправильный Redirect URI

**Симптомы:**
- Ошибка `redirect_uri_mismatch` (не `invalid_client`)
- Но может проявляться как `invalid_client` в некоторых случаях

**Решение:**
1. Проверьте Redirect URI в Google Cloud Console
2. Убедитесь, что он **ТОЧНО** совпадает с `GOOGLE_REDIRECT_URI` в Railway
3. Проверьте, что нет trailing slash

### Причина 5: OAuth Client деактивирован

**Симптомы:**
- Client ID существует в Console, но не работает
- Ошибка `401: invalid_client`

**Решение:**
1. В Google Cloud Console проверьте статус OAuth Client
2. Если он деактивирован, активируйте его
3. Или создайте новый Client ID

---

## 🔍 Дополнительная диагностика

### Проверка через Google OAuth Playground

1. Откройте [Google OAuth 2.0 Playground](https://developers.google.com/oauthplayground/)
2. Нажмите на иконку настроек (⚙️) в правом верхнем углу
3. Введите ваш Client ID и Client Secret
4. Попробуйте получить authorization code

Если это работает, значит проблема в конфигурации приложения, а не в Google.

### Проверка через curl

```bash
# Проверка status endpoint
curl -v https://testbackendvirtualtryon-production.up.railway.app/api/auth/google/status

# Проверка login endpoint
curl -v https://testbackendvirtualtryon-production.up.railway.app/api/auth/google/login
```

---

## ✅ Чеклист для исправления

- [ ] Проверил `/api/auth/google/status` - все параметры правильные
- [ ] Проверил переменные окружения в Railway - нет лишних пробелов
- [ ] Проверил Google Cloud Console - Client ID существует и активен
- [ ] Проверил Redirect URI в Console - совпадает с Railway
- [ ] Проверил логи Railway - нет ошибок инициализации
- [ ] Проверил authorization URL - открывается в браузере
- [ ] Перезапустил сервис после изменений переменных

---

## 📞 Если проблема не решена

1. **Соберите информацию:**
   - Ответ от `/api/auth/google/status`
   - Логи Railway (последние 100 строк с `[GOOGLE-AUTH]`)
   - Скриншот настроек OAuth Client в Google Cloud Console
   - Authorization URL (первые 200 символов)

2. **Проверьте:**
   - Что Client ID точно совпадает в Railway и Google Console
   - Что проект Google Cloud правильный
   - Что OAuth Consent Screen настроен правильно

3. **Попробуйте:**
   - Создать новый OAuth Client ID в Google Cloud Console
   - Обновить переменные в Railway
   - Перезапустить сервис

---

## 🔐 Безопасность

⚠️ **НИКОГДА не коммитьте:**
- Client Secret
- Полный Client ID в публичных местах
- Переменные окружения в Git

✅ **Используйте:**
- Railway Variables для production
- Маскированные значения в логах
- `.env` файл в `.gitignore` для локальной разработки

---

*Последнее обновление: после добавления диагностики OAuth*

