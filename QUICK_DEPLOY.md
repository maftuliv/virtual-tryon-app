# БЫСТРОЕ РАЗВЕРТЫВАНИЕ - 3 ВАРИАНТА

## 🚀 ВАРИАНТ 1: RAILWAY (САМЫЙ ПРОСТОЙ - 5 МИНУТ)

### Стоимость: $5/месяц (первый месяц бесплатно)

**Шаг 1:** Загрузите проект на GitHub
```bash
cd C:\Users\ivmaf\virtual-tryon-app
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/virtual-tryon-app.git
git push -u origin main
```

**Шаг 2:** Разверните на Railway
1. Откройте: https://railway.app
2. Войдите через GitHub
3. "New Project" → "Deploy from GitHub repo"
4. Выберите репозиторий `virtual-tryon-app`
5. Railway автоматически развернет приложение

**Шаг 3:** Получите URL
- Settings → Domains → Generate Domain
- Получите: `your-app.up.railway.app`

**Готово!** Приложение доступно!

---

## 🎯 ВАРИАНТ 2: HEROKU (10 МИНУТ)

### Стоимость: $5-7/месяц

**Шаг 1:** Установите Heroku CLI
- Скачайте: https://devcenter.heroku.com/articles/heroku-cli
- Установите на компьютер

**Шаг 2:** Войдите и создайте приложение
```bash
cd C:\Users\ivmaf\virtual-tryon-app
heroku login
heroku create your-app-name
```

**Шаг 3:** Разверните
```bash
git init
git add .
git commit -m "Deploy to Heroku"
git push heroku main
```

**Шаг 4:** Откройте
```bash
heroku open
```

**Готово!** Приложение на `your-app-name.herokuapp.com`

---

## 💻 ВАРИАНТ 3: VPS (30-60 МИНУТ, ПОЛНЫЙ КОНТРОЛЬ)

### Стоимость: $4-6/месяц

**Шаг 1:** Создайте VPS
1. Зарегистрируйтесь на DigitalOcean/Vultr/Hetzner
2. Создайте Droplet: Ubuntu 22.04, 1GB RAM
3. Получите IP адрес

**Шаг 2:** Подключитесь
```bash
ssh root@ВАШ_IP
```

**Шаг 3:** Загрузите проект
```bash
cd /var/www
git clone https://github.com/ваш-username/virtual-tryon-app.git
cd virtual-tryon-app
```

**Шаг 4:** Запустите автоматическое развертывание
```bash
sudo bash deployment/deploy_vps.sh
```

Скрипт автоматически:
- Установит все зависимости
- Настроит Nginx
- Создаст systemd сервис
- Запустит приложение

**Шаг 5:** Настройте домен
```bash
# Отредактируйте конфиг
nano /etc/nginx/sites-available/tryon

# Замените:
server_name your-domain.com www.your-domain.com;

# Установите SSL
apt install certbot python3-certbot-nginx -y
certbot --nginx -d your-domain.com -d www.your-domain.com
```

**Готово!** Приложение доступно по вашему домену!

---

## 📊 СРАВНЕНИЕ

| Характеристика | Railway | Heroku | VPS |
|----------------|---------|--------|-----|
| **Время**      | 5 мин   | 10 мин | 30-60 мин |
| **Сложность**  | ⭐      | ⭐⭐   | ⭐⭐⭐⭐ |
| **Цена**       | $5/мес  | $5-7/мес | $4-6/мес |
| **SSL**        | Авто ✅ | Авто ✅ | Ручная настройка |
| **Контроль**   | Средний | Средний | Полный |

---

## 🎯 РЕКОМЕНДАЦИЯ

### Начинающим → Railway
- Проще всего
- Быстрее всего
- Автоматически всё настраивает

### Есть опыт с Git → Heroku
- Хорошая документация
- Много туториалов
- Надежная платформа

### Нужен полный контроль → VPS
- Максимальная гибкость
- Можно всё настроить
- Дешевле на длинной дистанции

---

## 🌐 НАСТРОЙКА СВОЕГО ДОМЕНА

### Для Railway/Heroku:

1. **Купите домен** на Namecheap/Reg.ru (~$10/год)

2. **Настройте DNS:**
   - Зайдите в панель домена
   - Добавьте CNAME запись:
     - Host: `www`
     - Value: `your-app.railway.app` (или `.herokuapp.com`)

3. **В Railway/Heroku:**
   - Settings → Custom Domain
   - Добавьте ваш домен

### Для VPS:

1. **Купите домен**

2. **Настройте A-записи:**
   - Host: `@` → Value: `ВАШ_IP`
   - Host: `www` → Value: `ВАШ_IP`

3. **Установите SSL:**
```bash
certbot --nginx -d your-domain.com
```

---

## ✅ ПРОВЕРКА ПОСЛЕ РАЗВЕРТЫВАНИЯ

### 1. Откройте главную страницу
```
https://your-domain.com
```
Должен загрузиться интерфейс

### 2. Проверьте API
```
https://your-domain.com/api/health
```
Должен вернуть JSON: `{"status": "healthy", ...}`

### 3. Протестируйте загрузку
- Загрузите фото
- Нажмите "Примерить одежду"
- Проверьте что обработка работает

---

## 🆘 РЕШЕНИЕ ПРОБЛЕМ

### Railway/Heroku: Application Error

**Проверьте логи:**
```bash
# Heroku
heroku logs --tail

# Railway
Смотрите в веб-интерфейсе: Deployments → Logs
```

**Частые причины:**
- Не установлены зависимости → проверьте requirements.txt
- Порт неправильный → используйте `$PORT` переменную
- Python версия не поддерживается → проверьте runtime.txt

### VPS: 502 Bad Gateway

**Проверьте сервис:**
```bash
systemctl status tryon
journalctl -u tryon -n 50
```

**Перезапустите:**
```bash
systemctl restart tryon
systemctl restart nginx
```

### Домен не работает

**Подождите:**
- DNS может обновляться 5-60 минут

**Проверьте DNS:**
```bash
nslookup your-domain.com
```

**Или онлайн:**
- https://dnschecker.org

---

## 📞 ПОЛЕЗНЫЕ КОМАНДЫ

### Railway
```bash
# CLI установка (опционально)
npm install -g @railway/cli

# Логи
railway logs

# Переменные
railway variables
```

### Heroku
```bash
# Логи
heroku logs --tail

# Перезапуск
heroku restart

# Скейлинг
heroku ps:scale web=1
```

### VPS
```bash
# Статус сервиса
systemctl status tryon

# Перезапуск
systemctl restart tryon

# Логи
journalctl -u tryon -f

# Nginx логи
tail -f /var/log/nginx/tryon_error.log
```

---

## 🎉 ГОТОВО!

После развертывания ваше приложение:
- ✅ Доступно 24/7
- ✅ Работает по HTTPS
- ✅ Готово к использованию
- ✅ Можно подключить свой домен

**Выберите вариант и начинайте развертывание!** 🚀

---

*Для подробной инструкции смотрите: DEPLOYMENT_GUIDE.md*
