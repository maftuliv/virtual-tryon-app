# Tap to look - Next.js App

Современное веб-приложение для виртуальной примерки одежды с использованием искусственного интеллекта.

## Технологический стек

- **Next.js 15** - React фреймворк с App Router
- **React 19** - Последняя версия библиотеки
- **TypeScript 5.7** - Строгая типизация
- **Tailwind CSS 3.4** - Utility-first CSS фреймворк
- **Zustand 5.0** - Легковесное управление состоянием (1.2 KB)
- **SWR 2.2** - Hooks для работы с данными
- **Ky 1.7** - HTTP клиент (4 KB)
- **Framer Motion 11** - Анимации
- **date-fns 4** - Работа с датами

## Структура проекта

```
app-next/
├── src/
│   ├── app/              # Next.js App Router
│   │   ├── layout.tsx    # Root layout с SEO
│   │   └── page.tsx      # Главная страница
│   ├── components/       # React компоненты
│   │   ├── dashboard/    # Компоненты Dashboard
│   │   ├── Button.tsx
│   │   ├── Header.tsx
│   │   ├── Hero.tsx
│   │   ├── ImageUpload.tsx
│   │   └── TryonForm.tsx
│   ├── hooks/           # Custom React hooks
│   │   ├── useAuth.ts
│   │   ├── useImageUpload.ts
│   │   └── useTryons.ts
│   ├── lib/             # Утилиты и конфигурация
│   │   ├── api.ts       # API клиент
│   │   └── swr-config.ts
│   ├── store/           # Zustand stores
│   │   ├── auth.store.ts
│   │   └── tryon.store.ts
│   ├── styles/          # Глобальные стили
│   │   └── globals.css  # Tailwind + Glassmorphism
│   └── types/           # TypeScript типы
│       └── index.ts
├── public/              # Статические файлы
│   └── assets/          # Изображения и медиа
├── .env.local           # Переменные окружения
├── next.config.js       # Конфигурация Next.js
├── tailwind.config.ts   # Конфигурация Tailwind
├── tsconfig.json        # Конфигурация TypeScript
└── package.json         # Зависимости
```

## Установка и запуск

### Локальная разработка

1. **Установить зависимости:**
```bash
cd app-next
npm install
```

2. **Настроить переменные окружения:**

Файл `.env.local` уже создан с дефолтными значениями:
```env
NEXT_PUBLIC_API_URL=http://localhost:5000
```

3. **Запустить Flask backend** (в отдельном терминале):
```bash
cd ../backend
python run.py
```

4. **Запустить Next.js dev сервер:**
```bash
npm run dev
```

Приложение будет доступно на [http://localhost:3000](http://localhost:3000)

### Другие команды

```bash
# Production сборка
npm run build

# Запуск production сервера
npm start

# Проверка типов TypeScript
npm run type-check

# Линтинг кода
npm run lint

# Анализ размера бандла
ANALYZE=true npm run build
```

## Ключевые особенности

### 🎨 Glassmorphism дизайн

- Сохранен оригинальный дизайн с эффектом стекла
- Blur: 40-50px
- Прозрачность: 0.25-0.85
- Плавные анимации и переходы

### ⚡ Оптимизация производительности

- **Server Components** - 0 KB JavaScript для статических частей
- **Automatic Code Splitting** - загрузка только необходимого кода
- **Image Optimization** - автоматическая оптимизация изображений (AVIF, WebP)
- **SWR кеширование** - умное кеширование данных
- **Zustand** - минимальный overhead для state management

### 📱 Responsive дизайн

- Mobile-first подход
- Адаптивная сетка для всех экранов
- Оптимизированные изображения для разных устройств

### 🔒 Безопасность

- HTTPS заголовки (HSTS, X-Frame-Options, CSP)
- TypeScript для type-safety
- Защита от XSS и CSRF

## Dashboard функции

### Основные секции:

1. **User Greeting** - Приветствие пользователя
2. **Premium Banner** - Призыв к Premium подписке
3. **Liked Items** - Избранные примерки
4. **Recent Tryons** - Последние примерки
5. **Looks Section** - AI фильтры стиля (Casual, Business, Party, Sport, Beach)
6. **Brand Constructor** - Стили брендов (Zara, H&M, Mango)
7. **Style Plan** - Персональные рекомендации
8. **Service Updates** - Обновления сервиса

## API интеграция

### Основные endpoints:

```typescript
// Виртуальная примерка
POST /api/tryon
- personImage: File
- garmentImage: File
→ { id, result_url, r2_url, created_at }

// История примерок
GET /api/user/tryons
→ UserTryon[]

// Статистика
GET /api/user/tryons/stats
→ { total_tryons, favorite_count, recent_count }

// Избранное
POST /api/user/tryons/:id/favorite

// Обновление названия
PUT /api/user/tryons/:id/title
```

## Деплой на Railway

### ВАЖНО: Деплой только на STAGING!

```bash
# 1. Установить Railway CLI
npm i -g @railway/cli

# 2. Авторизоваться
railway login

# 3. Создать новый проект для staging
railway init

# 4. Установить переменные окружения
railway variables set NEXT_PUBLIC_API_URL=https://your-backend-staging.up.railway.app

# 5. Деплой на staging
railway up
```

### Переменные окружения для Railway:

```env
NEXT_PUBLIC_API_URL=https://virtual-tryon-backend-staging.up.railway.app
NODE_ENV=production
```

## Производительность

### Ожидаемые метрики:

- **Lighthouse Performance**: 95-100
- **LCP (Largest Contentful Paint)**: < 1.2s
- **FID (First Input Delay)**: < 100ms
- **CLS (Cumulative Layout Shift)**: < 0.1
- **Bundle Size**: ~85 KB (gzipped)

### Текущие оптимизации:

1. **Автоматическое разделение кода** - каждый маршрут загружается отдельно
2. **Tree-shaking** - удаление неиспользуемого кода
3. **Оптимизация пакетов** - date-fns, framer-motion используют tree-shaking
4. **Image optimization** - Next/Image с AVIF/WebP
5. **CSS минификация** - Tailwind JIT mode
6. **Кеширование** - SWR + HTTP caching headers

## Следующие шаги

1. ✅ Создать базовую структуру Next.js приложения
2. ✅ Настроить TypeScript, Tailwind, Zustand, SWR
3. ✅ Создать основные компоненты (Header, Hero, TryonForm)
4. ✅ Мигрировать Dashboard с glassmorphism
5. ✅ Проверить dev сборку
6. ⏳ Добавить авторизацию (модальное окно)
7. ⏳ Интегрировать с Flask backend API
8. ⏳ Добавить обработку результатов примерки
9. ⏳ Создать страницу отдельного Dashboard
10. ⏳ Добавить модальные окна для Looks, Brand Constructor, Style Plan
11. ⏳ Протестировать с реальным backend
12. ⏳ Production сборка и оптимизация
13. ⏳ Деплой на Railway STAGING

## Техническая поддержка

При возникновении проблем:

1. Проверьте, что Flask backend запущен на порту 5000
2. Проверьте переменные окружения в `.env.local`
3. Очистите кеш: `rm -rf .next && npm run dev`
4. Проверьте логи в консоли браузера

## Лицензия

Proprietary - Tap to look © 2024
