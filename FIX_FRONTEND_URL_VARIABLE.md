# 🔧 Исправление проблемы с FRONTEND_URL в Railway

## ❌ Проблема

При добавлении переменной `FRONTEND_URL` в Railway возникает ошибка:
```
ERROR: failed to build: failed to solve: secret FRONTEND_URL: not found
```

Railway пытается использовать переменную как секрет во время сборки, но это обычная переменная окружения.

---

## ✅ Решение

### Вариант 1: Удалить и добавить заново (РЕКОМЕНДУЕТСЯ)

1. **Удалите переменную из Railway:**
   - Railway Dashboard → `test_backend_virtual_tryon` → Variables
   - Найдите `FRONTEND_URL`
   - Удалите её (или временно переименуйте)

2. **Задеплойте без переменной:**
   - Railway автоматически перезапустит сервис
   - Деплой должен пройти успешно

3. **Добавьте переменную заново:**
   - После успешного деплоя добавьте `FRONTEND_URL` обратно
   - **ВАЖНО:** Убедитесь, что добавляете как **обычную переменную**, а не как **секрет**
   - Значение: `https://testtaptolooknet-production.up.railway.app`

### Вариант 2: Использовать без переменной (работает с fallback)

Переменная `FRONTEND_URL` **опциональная** - код работает и без неё!

**Fallback логика:**
- Если `FRONTEND_URL` не установлена, используется:
  1. `Origin` заголовок из запроса (если есть)
  2. Или дефолтное значение: `https://testtaptolooknet-production.up.railway.app`

**Можно просто не добавлять переменную** - всё будет работать с fallback.

---

## 🔍 Проверка после исправления

После удаления переменной и успешного деплоя:

1. **Проверьте статус:**
   ```bash
   curl https://testbackendvirtualtryon-production.up.railway.app/api/auth/google/status
   ```

2. **Протестируйте OAuth:**
   - Откройте: https://testtaptolooknet-production.up.railway.app
   - Нажмите "Войти через Google"
   - Должно работать с fallback URL

---

## 📝 Примечание

Переменная `FRONTEND_URL` нужна только если:
- У вас несколько фронтендов
- Нужно явно указать URL для редиректа
- Fallback не подходит для вашего случая

В большинстве случаев **fallback работает автоматически** и переменная не нужна.

---

*Инструкция для исправления ошибки сборки Railway*


